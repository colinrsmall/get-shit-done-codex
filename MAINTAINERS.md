# Maintainers

This repo is not published to npm and has no installer/update mechanism.

Distribution is manual: users copy `commands/`, `agents/`, and `get-shit-done/` into `~/.config/opencode/`.

When you make user-facing changes:

- Update `commands/gsd-help.md` (source of truth for command usage)
- Update `README.md` if install instructions change
